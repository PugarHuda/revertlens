# Optional. Enables the AI long-tail explainer (src/ai/explain.ts) for reverts
# that have no verified knowledge-base answer. Without it, RevertLens still works
# fully via the deterministic precompile linter + live on-chain revert decoding —
# it just won't add AI-inferred explanations for the long tail.
ANTHROPIC_API_KEY=
